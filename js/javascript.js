function navFunction() {
      var a = document.getElementById("navigation");
      if (a.className === "nav") {
        x.className += " responsive";
      } else {
        x.className = "nav";
      }
